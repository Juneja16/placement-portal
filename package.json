{
  "name": "placement-portal",
  "version": "1.0.0",
  "scripts": {
    "install-client": "npm install --prefix Client/frontend",
    "install-server": "npm install --prefix Server",
    "build": "npm run install-client && npm run build --prefix Client/frontend",
    "start": "node Server/server.js",
    "deploy": "npm run install-server && npm run build && npm run start"
  }
}
